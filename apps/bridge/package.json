{
  "name": "cloudy-codex-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "cloudy-bridge": "src/cli.js" },
  "scripts": {
    "start": "node src/cli.js run",
    "schema:generate": "codex app-server generate-json-schema --out schema",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=20" }
}
