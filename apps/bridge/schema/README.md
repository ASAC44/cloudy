# Codex app-server protocol snapshot

Generated from `codex-cli 0.144.5` with:

```sh
pnpm schema:generate
```

The bridge rejects older Codex CLI versions. Regenerate and review this directory before raising the minimum supported version.
