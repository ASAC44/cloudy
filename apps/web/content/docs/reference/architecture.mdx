---
title: Architecture and technology
description: The shipped hardware, services, data flows, and security boundaries that make up Cloudy.
---

This page describes the current implementation. Future context, memory, and
integration ideas belong in [Product vision](/docs/product/vision).

## Runtime components

| Component | Runtime | Responsibility |
| --- | --- | --- |
| Web | Next.js 16, React 19, Tailwind CSS 4 | Public site, Fumadocs pages, Supabase login, and authenticated dashboard |
| API | Hono on Node.js | Authenticated HTTP and SSE endpoints, Pod and bridge protocols, OAuth callbacks, AI settings, rule polling and evaluation, approved delivery, and atomic approval transitions |
| Pod | Python 3.13, pygame-ce, gpiozero | Pairing, request display, physical decisions, cached active request, audio capture, and realtime invalidation handling |
| Codex bridge | Local Node.js process | Local `codex app-server` session, selected workspace access, and approval exchange |
| Data store | Supabase Postgres and Auth | Canonical users, credentials, Pods, requests, decisions, integrations, rules, Codex state, and audit records |
| Memory service | Python 3.13, Graphiti 0.29.2, Neo4j 5.26 | Private derived temporal graph ingestion, owner-scoped search, deletion, and rebuild |

Local development can temporarily persist Pod screen layouts in SQLite when
the matching Supabase migration has not been applied. This compatibility path
is enabled only by the development launcher; production keeps Postgres as the
sole authoritative store and fails closed on schema mismatch.

The web and API are separate processes. The Hono process runs a guarded,
non-overlapping background drain loop; database leases recover abandoned work
after restarts and coordinate multiple API replicas.

## Pod runtime

The hardware target is a Raspberry Pi Zero 2 W with a 640×480 touchscreen,
Approve and Reject buttons, a microphone, Wi-Fi, and no speaker. Raspberry Pi
OS Lite launches pygame under Cage as a systemd service.

The Pod opens an authenticated SSE stream using a revocable Pod-scoped bearer
token. PostgreSQL triggers publish metadata-only invalidations through a private
Supabase Realtime channel after the surrounding transaction commits. Each API
replica fans matching invalidations into its connected Pod streams. Events are
only wake-up signals: on connect, invalidation, or reconnect, the Pod fetches
`/v1/pod/requests/current` as the authoritative snapshot. It polls every 10
seconds while realtime is degraded and performs a 90-second safety refresh
while connected. Each snapshot includes the saved three-screen layout; the
dashboard autosaves drag-and-drop changes, and the Pod applies the next
snapshot.
Screen 2 is the default, with horizontal swipes opening Screens 1 and 3. Each
screen subscribes to up to six official apps or Custom MCP feeds. Attachments
only route Pings: they are not rendered as rows or grouped by provider. The
active queue selects one Ping at a time, and each Ping opens its assigned
screen. Swipe-up opens its details, and swipe-down returns to its summary. Any
screen without an active Ping renders Cloudy immediately. The Pod stores its
token and active request in its state directory with mode `0600`; Supabase,
OAuth, AI, and integration credentials never reach the device. If the network
drops, the cached request remains readable but decisions stay disabled until
the API is reachable.

Home mascot controls queue one ephemeral Blink, Yawn, Sleep, or Jump command
for the paired Pod. A local invalidation wakes the paired Pod, whose next
authenticated snapshot consumes that command once, and
the pygame renderer temporarily overlays the animation without changing the
active request. These playful commands are intentionally process-local: a
server restart may drop an undelivered animation, but cannot affect approval
state or user data.

After a dashboard decision commits, the API queues the approved or rejected
outcome as one-shot Pod metadata and publishes a realtime invalidation. The Pod
consumes that outcome from its authoritative snapshot, plays the matching
animation, and returns to idle. The database decision remains authoritative if
the ephemeral animation is missed.

Development mock Pings store only their selected provider type and screen with
the ordinary request. The API adds fixed, credential-free GitHub, Gmail,
general-alert, or Codex-plan presentation data to the authenticated Pod
snapshot. A dictated revision to the Codex mock stays local to the Pod; it
exercises recording and transcription without impersonating a real bridge
command.

The microphone is captured with ALSA `arecord` as mono 16 kHz, 16-bit PCM WAV,
limited to 30 seconds. Tapping one button approves or rejects; holding both for
600 ms records, and releasing either stops. Short two-button chords are ignored.
The transcript is displayed and requires a separate approval before it becomes
a Codex instruction. The bridge steers an active turn and starts a read-only
planning turn when the selected conversation is idle.

## Web and API boundary

Browser authentication terminates at Supabase Auth. Next.js server actions
forward the user's Supabase access token to Hono; the API validates signature,
expiry, issuer, and audience before using the user identity. Dashboard pages do
not receive the Supabase secret key or decrypted provider credentials. A
dashboard Approve or Reject action resolves only the current owner-scoped Ping
through the active Pod and the same transactional `decide_approval` operation
used by physical Pod decisions.

The public API also has separate authentication boundaries:

- Pod pairing creates a one-time code and a scoped Pod token.
- Automation clients use revocable automation keys.
- Codex bridges use a separately paired, revocable bridge token.
- OAuth callbacks validate short-lived, one-time state before saving encrypted
  credentials.

`CONNECTION_ENCRYPTION_KEY` encrypts provider, AI, Codex-interaction, and rule
payload secrets before storage. Retain the same key across deployments;
changing it makes existing encrypted values unreadable.

## Connections and dynamic rules

The shipped connection providers are GitHub, Gmail, Google Calendar, Vercel,
Telegram, Linear, Stripe, Notion, and a custom remote MCP endpoint. GitHub,
Gmail, Google Calendar, and Notion use OAuth; Vercel, Linear, and Stripe accept encrypted bearer
credentials; Telegram supports an authenticated bot webhook or user
authorization; and custom MCP connections support configured authentication.
Bot updates enter the API directly, while personal-account events are
maintained by the worker. Linear, Stripe, and Notion use fixed official Streamable HTTP
endpoints. Google Calendar uses bounded REST reads plus approval-gated event
creation and etag-guarded updates behind the same capability interface.
Gmail requests `gmail.readonly` plus `gmail.send`; reply delivery is exposed only
as an exact, hash-bound action after the user approves the complete email and
response on the Pod.

The rule builder uses the chosen AI provider to turn a conversation into a
reviewable rule definition. Supported providers are OpenAI, Anthropic,
Cerebras, OpenRouter, and an OpenAI-compatible custom endpoint. Model names are
user-configured; Cloudy does not assume a default text model.

After activation, the API runtime polls or listens to the source connection,
deduplicates events in Postgres, evaluates the saved rule, and creates an exact
approval request. An approved request is delivered through the configured
action connection or workflow callback. Database constraints, transactions,
payload hashes, and idempotency keys prevent stale or duplicate decisions and
deliveries.

## Codex and voice flow

The bridge runs beside local repositories and talks to `codex app-server` over
local standard input and output. Local paths and Codex credentials remain on
that computer; Cloudy stores only opaque workspace identifiers, display labels,
thread state, and encrypted approval payloads.

1. The Pod records audio and uploads the WAV file over HTTPS.
2. The API decrypts the user's official OpenAI API key and transcribes with
   `gpt-4o-mini-transcribe`.
3. The user approves the displayed transcript.
4. The bridge steers the exact active turn, or starts or resumes the selected
   Codex thread in a read-only planning sandbox when no turn is active.
5. Codex returns the resulting plan to the Pod as an approval request.
6. After approval, the bridge verifies the repository has not changed and
   starts implementation with workspace write access.
7. Command, file-change, and permission requests continue through the same
   exact-payload approval path.

Other configured AI providers can power the rule builder, but voice currently
requires the official OpenAI endpoint because the transcription route calls
OpenAI's audio API directly.

## Shared agent memory

Cloudy stores small, user-owned memories in Supabase so the rule builder,
integration runtime, and Codex prompts can reuse relevant context across
sessions. Memories may be scoped to a user, workspace, or provider and can
include a source pointer. They are bounded context only: they never authorize
an action, replace a provider read, or bypass Pod approval.

The Ping runtime also keeps a normalized, owner-scoped learning ledger for
people, verified channel identities, decisions, writing examples, preferences,
provider import cursors, and future graph references. Sensitive profiles,
identities, message examples, and decision situations are encrypted. Row-level
security and owner constraints prevent records from linking across users.

Approval and delivery are separate facts. The exact approval transition writes
the decision and a durable memory-outbox event in the same Postgres transaction.
Delivery later strengthens the precedent only if the provider confirms success.
Outbox events are idempotent and leased. The worker preserves per-owner event
order, writes metadata-only temporal episodes to Graphiti, and stores the
derived graph reference atomically when it completes an event. Recent unindexed
decisions provide read-through consistency while the graph catches up.

The runtime retrieves bounded owner-scoped action and voice evidence. Existing
version-2 Pings remain fixed. Version-3 learned communication Pings can select
only an opaque candidate from a normalized, owner-scoped allowlist containing
Gmail and Telegram writes. The server, not the model, binds the capability,
connection, current-event recipient or verified identity, and exact arguments.
Unknown candidates, low confidence, identity drift, capability drift, and graph
outages all cause learned-action abstention. No selection authorizes delivery;
the hash-bound Pod approval remains authoritative. The service derives every
namespace from the authenticated owner UUID, accepts only a fixed Cloudy
ontology, disables Graphiti telemetry and raw episode storage, and exposes no
raw Cypher. Its internal routes require timestamped, nonce-protected HMAC
signatures. This boundary is documented in
[Graph memory service](/docs/development/graph-memory).

The API exposes explicit authenticated endpoints: `POST /v1/memories` to
write a memory, `GET /v1/memories?q=...&limit=...` to list memories, and
`DELETE /v1/memories/:id` to soft-delete one. Workspace and provider memories
also require their scope identifier; provider memories require `provider`.
Integrations do not silently save arbitrary conversation text.

`PUT /v1/settings/personalization` controls collection and prompt injection.
For editable Ping replies, `GET /v1/requests/:id/reply` returns the current
exact text and hash, while `PUT /v1/requests/:id/reply` atomically replaces the
encrypted draft, encrypted action, payload hash, and encrypted pending
correction. The correction becomes learning evidence only after approval. A Pod
holding the previous hash cannot approve the changed action.

Reply prompts receive no more than 12 explicit memories, bounded graph and
read-through evidence, plus five eligible approved writing examples, each
under a bounded character budget. Examples are ranked by identity/person,
channel, intent, connection, approval correction, and confirmed delivery before
falling back to global voice.

Context capabilities remain fixed at rule setup. Each normalized binding is
required or optional and activates always, for scheduling intent, for a selected
recipient, or for a selected thread. Required failures stop drafting; optional
failures appear as warnings. Calendar event reads wait for structured
scheduling intent, current calendar data overrides memory, and missing dates,
duration, or timezone produce a clarification instead of a guessed slot.

## Decision guarantees

- A request can leave `pending` only once.
- Decisions include the displayed payload hash and an idempotency key.
- Expired, revoked, changed, or already-resolved requests fail closed.
- Approved provider actions and callbacks use durable delivery identities.
- Learned communication candidates are capped at eight and revalidated before
  approval creation and again before delivery.
- Test Pings record a decision but deliberately execute no external action.
- Sensitive credentials stay encrypted server-side and are never returned by
  read endpoints.

## Deployment topology

```text
Browser ───────────────> Next.js web/docs ────────┐
                                                  v
Automation client ───────────────────────────> Hono API <────> Supabase
Pod <════════ authenticated SSE invalidations ═══> ^              ^
Pod ───────── snapshots, decisions, audio ────────> ^              ^
Codex bridge <──── local stdio ────> Codex CLI ───┘              |
                                                                 |
Telegram / integrations <──────── Hono runtime ──────────────────┘
```

Railway hosts the web and API processes. The Raspberry Pi and Codex
bridge run outside Railway and therefore require the API's public HTTPS origin.
See [Railway deployment](/docs/deployment/railway) for process commands and
variables.

## References

- [OpenAI GPT-4o mini Transcribe](https://developers.openai.com/api/docs/models/gpt-4o-mini-transcribe)
- [OpenAI speech to text](https://developers.openai.com/api/docs/guides/speech-to-text)
- [Raspberry Pi I²S peripherals](https://pip-assets.raspberrypi.com/categories/1259-audio-camera-and-display/documents/RP-009699-WP-1-Using%20the%20I2S%20peripherals%20on%20Raspberry%20Pi%20SBCs.pdf)
- [TDK INMP441 data sheet](https://product.tdk.com/system/files/dam/doc/product/sw_piezo/mic/mems-mic/data_sheet/inmp441.pdf)
- [pygame-ce events](https://pyga.me/docs/ref/event.html)
- [Hono on Node.js](https://hono.dev/docs/getting-started/nodejs)
- [Supabase Auth](https://supabase.com/docs/guides/auth)
- [Model Context Protocol](https://modelcontextprotocol.io/docs/getting-started/intro)
